# Server Configuration
PORT=3000

# JWT Authentication
# IMPORTANTE: Troque essa chave por uma segura em produção!
# Você pode gerar uma chave aleatória com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=seu-secret-key-super-seguro-aqui
JWT_EXPIRES_IN=7d
